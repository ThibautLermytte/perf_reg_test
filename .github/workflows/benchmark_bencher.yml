name: BENCHMARK_BENCHER
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:
  benchmark_base_branch:
    name: Continuous Benchmarking with Bencher
    permissions:
      checks: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: bencherdev/bencher@main
      - name: Track base branch benchmarks with Bencher
        run: |
          g++ benchmark_game.cpp play_game.cpp -std=c++11 -isystem include/  -L. -lbenchmark -lpthread -o mybenchmark
          bencher run \
          --project thibaut-lermytte-s-project \
          --token '${{ secrets.BENCHER_API_TOKEN }}' \
          --branch main \
          --testbed ubuntu-latest \
          --threshold-measure latency \
          --threshold-test t_test \
          --threshold-max-sample-size 64 \
          --threshold-upper-boundary 0.99 \
          --thresholds-reset \
          --err \
          --adapter cpp_google "./mybenchmark --benchmark_format=json" \
          --github-actions '${{ secrets.GITHUB_TOKEN }}' \
